# Environment Configuration Example
# 
# This project uses environment-specific files with the following loading order:
# 1. .env.local (local development defaults) - loaded in development
# 2. .env.production (production defaults) - loaded in production  
# 3. .env.staging (staging defaults) - loaded in staging
# 4. .env (overrides) - always loaded last, overrides environment-specific files
#
# Copy this file to .env and uncomment/modify values to override defaults

# API Configuration
# Base URL for the backend API server
# Default: http://localhost:8080 (from .env.local)
# VITE_API_BASE_URL=http://localhost:8080

# Examples for different environments:
# Development: http://localhost:8080 (default from .env.local)
# Production: https://api.yourdomain.com (default from .env.production)
# Staging: https://staging-api.yourdomain.com (default from .env.staging)
# Custom override: uncomment above to override any environment default

# Environment-specific settings
# VITE_NODE_ENV=development
