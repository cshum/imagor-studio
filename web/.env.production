# Production defaults
# This file contains default values for production environment
# These values will be used unless overridden by .env

# API Configuration
# Empty base URL means relative paths (same origin as frontend)
VITE_API_BASE_URL=

# Production specific settings
VITE_NODE_ENV=production
